{
  // Keep global defaults; relax only for template source files
  "overrides": [
    {
      "files": ["src/claude_builder/templates/**"],
      "config": {
        // Long lines are common in templated content; allow them
        "MD013": false
      }
    }
  ]
}
